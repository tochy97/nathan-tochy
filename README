Git Commands

To add new file: git add 'filepatch/filename.file'
To track changes: git commit -am 'message about what you changed'
To send to repository to master(the repository): git push origin master
